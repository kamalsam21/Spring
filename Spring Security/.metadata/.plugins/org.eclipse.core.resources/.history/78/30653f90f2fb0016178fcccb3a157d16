<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<body>
	<h1>HTTP Status 403 - Access is denied</h1>

	<div th:if="${username}">
		<h2 th:text="${'Username' + username}">You do not have permission to access this page!</h2>
	</div>
	
	<h2 th:if="${username}" th:text>Username : ${username} <br/>
            You do not have permission to access this page!</h2>

</body>
</html>